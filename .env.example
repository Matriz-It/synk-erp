# URL base da API NestJS (sem barra no final)
API_URL=http://localhost:3000/api/v1

# Mercado Pago — chave pública, exposta no navegador (tokenização de cartão)
NEXT_PUBLIC_MP_PUBLIC_KEY=TEST-xxxxxxxx
